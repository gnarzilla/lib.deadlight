# Native C Modules

The `c/` directory contains early shared native modules for Deadlight projects.

Current areas:

- `c/auth/` — auth primitives and stubs for future vault integration
- `c/network/` — shared networking primitives such as connection pooling

These modules are not yet a stable public C API.
